# Документация на CVRP Optimizer / Bizant 2.0

Тази директория съдържа актуалната документация на приложението. Описанията са сверени с текущия source код, а не със стар build или стара конфигурация.

## Кой документ да отворя

| Ако искаш да... | Отвори |
|---|---|
| инсталираш, стартираш или направиш build | [Инсталация, build и изходни файлове](INSTALLATION_BUILD_OUTPUTS.md) |
| настроиш програмата през Desktop GUI или да разбереш ежедневния workflow | [Ръководство за работа](USER_GUIDE.md) |
| извикаш `/run`, `/run_saturday`, `/solve`, `/tsp` или Web интерфейса | [HTTP API и Web интерфейс](API_AND_WEB.md) |
| избереш решител или да разбереш ограниченията | [Решители, цели и ограничения](SOLVERS_AND_CONSTRAINTS.md) |
| намериш точно поле от `config.py` | [Справочник на конфигурацията](CONFIGURATION_REFERENCE.md) |
| разбереш програмата без технически подробности | [Обяснение на програмата](../ОБЯСНЕНИЕ_НА_ПРОГРАМАТА.md) |
| проследиш вътрешния поток на данните | [Работни процеси и алгоритми](../WORKFLOWS_DOCUMENTATION.md) |
| настроиш изолирания PyVRP 0.14 worker | [PyVRP 0.14 backend](../PYVRP_EXPERIMENTAL.md) |

## Източник на истината

- `config.py` определя постоянните настройки и допустимите solver идентификатори.
- `GET /health` връща активните endpoint-и, `commands` и `settings_schema` за стартирания build.
- Desktop GUI записва постоянните настройки в `config.py` и създава backup.
- Web GUI различава временни настройки за един рън, глобални настройки и отделно записване само на бусовете. Наследеният `run-defaults` endpoint още съществува, но текущата страница не го прилага при старт.
- Настройки, подадени към публичните `/run` и `/solve`, са временни за конкретната заявка и не записват `config.py`.

Когато документацията и стар build се различават, провери кой EXE и кой `config.py` реално са стартирани. Новите функции се появяват в build само след ново изграждане и разпространяване на цялата `dist` директория.

## Термини

- **Физически бус** — едно реално превозно средство с общи дневни лимити.
- **Маршрут/курс** — едно излизане на физическия бус от депо до крайна точка. При включени втори курсове един бус може да има няколко курса.
- **Run/рън** — едно пълно изпълнение: вход, матрица, solver, файлове и по желание `setData`.
- **Текущи настройки** — стойности, използвани само от един Web/API рън.
- **Legacy Web defaults** — старо поле/endpoint, което текущата Web страница не предлага и не смесва автоматично в ръна; не разчитай на него.
- **Глобални настройки** — стойности, записани в `config.py` и използвани от Desktop, API и Web според описанията в съответните документи.
